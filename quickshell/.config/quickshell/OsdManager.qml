// OsdManager — no longer used; kept as an empty stub so Quickshell
// does not error if any cached reference to this singleton exists.
pragma Singleton

import Quickshell
import QtQuick

Singleton {}
