{
  plugins.trouble = {
    enable = true;
  };

  plugins.which-key = {
    enable = true;
  };

  plugins.grug-far = {
    enable = true;
    settings = {
    };
  };

  plugins.noice = {
    enable = true;
    autoLoad = true;
  };

  plugins.mini-pairs = {
    enable = true;
  };

  plugins.ts-comments = {
    enable = true;
  };

  plugins.gitsigns = {
    enable = true;
  };

  plugins.todo-comments = {
    enable = true;
  };

  plugins.neoscroll = {
    enable = true;
    autoLoad = true;
  };

  plugins.colorizer = {
    enable = true;
    autoLoad = true;
  };

  plugins.dashboard = {
    enable = true;
    autoLoad = true;
  };

  plugins.nix = {
    enable = true;
    autoLoad = true;
  };
}
